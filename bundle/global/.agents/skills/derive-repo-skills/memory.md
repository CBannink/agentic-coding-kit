# Derive Repo Skills — Accumulated Knowledge

Cross-repo patterns confirmed across multiple derive-repo-skills sessions.
**Scope**: global skill-level (not repo-specific).

## Write Gate
Before adding an entry ask: "Would this convention-extraction or repo-skill-generation pattern help a future session in a different repo?"
If no -> write to the session private handoff instead.

## Format
```
- [DATE] [H/M/L confidence] [domain: triage|history|routing|skill-synthesis]
  Pattern: {one-line}
  Evidence: {where confirmed}
  Apply when: {trigger condition}
```

Max 20 entries. Promote to derive-repo-skills/SKILL.md if confirmed HIGH in 3+ repos.

---

## Confirmed Patterns

*(empty — seed file; populate from confirmed multi-repo patterns)*
