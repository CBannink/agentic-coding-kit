# Analyze Skill — Accumulated Knowledge

Cross-repo patterns confirmed across multiple /analyze sessions.
**Scope**: global skill-level (not repo-specific).

## Write Gate
Before adding an entry ask: "Would this pattern help a future /analyze session in a completely different repo?"
If no → write to the session private handoff instead.

## Format
```
- [DATE] [H/M/L confidence] [domain: architecture|tradeoffs|research|performance|security]
  Pattern: {one-line}
  Evidence: {where confirmed — repo or session context}
  Apply when: {trigger condition}
```
Max 20 entries. Promote to analyze/SKILL.md if confirmed HIGH in 3+ repos.

---

## Confirmed Patterns

*(empty — seed file; populate from confirmed multi-repo patterns)*
