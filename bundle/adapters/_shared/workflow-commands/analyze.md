# /analyze

Read and follow `__SKILL_ROOT__/analyze/SKILL.md` exactly.

__HOST_NAME__ adapter note:

1. This command is a workflow entrypoint, not a general chat shortcut.
2. Use `/analyze` for actual feature, idea, architecture, or wedge evaluation
   that benefits from multiple expert perspectives — not for simple questions.
3. The expert discussion is **orchestrator-mediated**: build one evidence packet,
   collect expert openings, run one response round with peer summaries, then
   verify factual claims.
4. Keep facts separate from recommendations and avoid reopening broad inline
   excavation when the delegated exploration already covered it.

Run multi-angle exploration and synthesis.

Keep facts separate from recommendations and verify important claims.
